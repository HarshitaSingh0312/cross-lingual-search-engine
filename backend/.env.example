DATABASE_URL=postgresql://user:password@ep-xxxx.neon.tech/dbname?sslmode=require
JWT_SECRET_KEY=replace-with-a-long-random-value-e.g.-python -c "import secrets; print(secrets.token_hex(32))"
# From console.anthropic.com - prepaid credits, no auto-reload. Leave blank to disable
# POST /search/rag (it returns 503 instead of crashing the app).
ANTHROPIC_API_KEY=
